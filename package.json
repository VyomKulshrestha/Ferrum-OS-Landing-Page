{
  "name": "ferrum-os-landing-page",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "evidence:sync": "node scripts/sync-public-evidence.mjs ../cursor-os-base public",
    "test": "node --test tests/*.test.mjs",
    "verify:media": "node scripts/verify-media.mjs",
    "verify:dist": "node scripts/verify-dist.mjs",
    "check": "npm run test && npm run verify:media && npm run build && npm run verify:dist"
  },
  "devDependencies": {
    "ffmpeg-static": "^5.2.0",
    "vite": "^7.1.1"
  },
  "dependencies": {
    "@vercel/functions": "^3.9.3"
  }
}
